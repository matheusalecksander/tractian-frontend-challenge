import { TreeViewChild } from "./Child";
import { TreeViewParent } from "./Parent";
import { TreeViewRoot } from "./Root";

export const TreeView = {
	Root: TreeViewRoot,
	Parent: TreeViewParent,
	Child: TreeViewChild,
};
